replace race = 0 if race == .
replace y = 0 if y == .
replace ind = 0 if ind == .
replace occ = 0 if occ == .
replace fweeksgap = 0 if fweeksgap == .
replace numjobsyear = 0 if numjobsyear == .
replace dayspaidleave = 0 if dayspaidleave == .
replace hrsperweek1 = 0 if hrsperweek1 == .
replace dumhadpreggap = 0 if dumhadpreggap == .
replace paidgapmarker = 0 if paidgapmarker == .
replace birthgap = 0 if birthgap == .
replace childyrgap = 0 if childyrgap == .

